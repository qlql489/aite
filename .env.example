# Claude CLI 路径（可选）
# 如果不配置，系统会自动搜索常见位置
# CLAUDE_CLI_PATH=/usr/local/bin/claude

# 日志级别（可选，默认: info）
# 可选值: debug, info, warn, error
LOG_LEVEL=info

# 注意：API Key 由 claude-cli 管理
# 请确保 claude-cli 已正确配置 API Key
# 配置方式：运行 `claude` 命令并按照提示配置
